package edu.nju.express.businesslogic.strategybl.organizationbl.Mock;

import static org.junit.Assert.assertEquals;

import org.junit.Test;

import edu.nju.express.businesslogic.strategybl.organizationbl.OrganizationBl;
import edu.nju.express.common.ResultMessage;

public class OrganizationTester {
/*
	@Test
	public void test(){
		OrganizationBl bl = new OrganizationBl(new MockStation(), new MockHall());
		assertEquals(bl.createStation("025", "NJ"),ResultMessage.INVALID);
		assertEquals(bl.createStation("028", "Unknown"),ResultMessage.VALID);
		
		assertEquals(bl.createHall("025001", "NJ"),ResultMessage.INVALID);
		assertEquals(bl.createHall("028001", "Unknown"),ResultMessage.VALID);
	}
	*/
}
